{{ target: scales-quantile }}

# scales.quantile

## type(string) = 'quantile'

Set the scale type to `quantile`

{{ use: scales-common-domain(prefix = '#') }}

{{ use: scales-common-range(prefix = '#') }}