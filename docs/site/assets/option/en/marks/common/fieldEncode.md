{{ target: marks-common-fieldEncode }}

The type definition is:

```ts
type FieldEncodeType = {
  field: string;
};
```

The `field` setting reads the value from the corresponding field in the related data.