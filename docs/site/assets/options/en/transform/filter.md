{{ target: transform-filter }}

## transform.filter(Object)

Implement filter transformation on data

### type(string) = filter

Set the data transformation type to `filter`

### callback

The callback function for performing filtering, return a boolean value

{{ use: common-callback() }}