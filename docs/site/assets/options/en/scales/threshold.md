{{ target: scales-threshold }}

# scales.threshold

## type(string) = 'threshold'

Set the scale type to `threshold`

{{ use: scales-common-domain(prefix = '#') }}

{{ use: scales-common-range(prefix = '#') }}