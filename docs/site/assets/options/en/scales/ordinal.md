{{ target: scales-ordinal }}

# scales.ordinal

## type(string) = 'ordinal'

Set the scale type to `ordinal`

{{ use: scales-common-domain(prefix = '#') }}

{{ use: scales-common-range(prefix = '#') }}