{"type":{"desc":"<p>将数据变换类型设置为 <code class=\"codespan\">sunburst</code>，主要用于旭日图的展示</p>\n"},"startAngle":{"desc":"<p>旭日图的起始角度，默认值为<code class=\"codespan\">Math.PI / 2</code>，也就是时钟 12 点的方向</p>\n"},"endAngle":{"desc":"<p>旭日图的起始角度，默认值为 <code class=\"codespan\">(-3 * Math.PI) / 2</code></p>\n"},"center":{"desc":"<p>圆心的位置，默认值为<code class=\"codespan\">[&#39;50%&#39;, &#39;50%&#39;]</code>，也就是画布的中心点\n值的类型为长度为 2 的数组，数组的元素支持两种格式：</p>\n<ul>\n<li><code class=\"codespan\">number</code> 数值类型，指定中心点的坐标</li>\n<li><code class=\"codespan\">string</code> 百分比字符串，<code class=\"codespan\">center[0]</code>相对于画布的宽度计算一个坐标，<code class=\"codespan\">center[1]</code>相对于画布的高度计算一个坐标</li>\n</ul>\n"},"innerRadius":{"desc":"<p>最小内径对应的值，支持三种格式：</p>\n<ul>\n<li><code class=\"codespan\">number</code> 数值类型，指定最内层的内径的大小，单位为<code class=\"codespan\">px</code></li>\n<li><code class=\"codespan\">string</code> 百分比字符串，指定所有层级的最小内径，相对于画布的宽度、高度的最小值，按照百分比，计算一个内径的大小</li>\n<li><code class=\"codespan\">Array&lt;number|string&gt;</code> 数组类型中的每个元素都支持数值类型和百分比类型，数组用于设置每个层级层级的内径大小</li>\n</ul>\n"},"outerRadius":{"desc":"<p>最大外径对应的值，支三种格式：</p>\n<ul>\n<li><code class=\"codespan\">number</code> 数值类型，指定最外层的内径的大小，单位为<code class=\"codespan\">px</code></li>\n<li><code class=\"codespan\">string</code> 百分比字符串，指定最外层的内径的大小，相对于画布的宽度、高度的最小值，按照百分比，计算一个外径的大小</li>\n<li><code class=\"codespan\">Array&lt;number|string&gt;</code> 数组类型中的每个元素都支持数值类型和百分比类型，数组用于设置每个层级层级的外径大小</li>\n</ul>\n"},"gapRadius":{"desc":"<p>设置相邻层级之间的半径间距，支持两种格式：</p>\n<ul>\n<li><code class=\"codespan\">number</code> 数值类型，指定相邻层级之间的半径间距，单位为<code class=\"codespan\">px</code></li>\n<li><code class=\"codespan\">number[]</code> 数组类型，<code class=\"codespan\">gapRadius[i]</code>表示第<code class=\"codespan\">i</code>层和第<code class=\"codespan\">i+1</code>层之间间隔的半径大小</li>\n</ul>\n"},"nodeKey":{"desc":"<p>类型：<code class=\"codespan\">string|number</code>\n设置节点的全局唯一 key 对应的字段，如果不设置，内部会根据层级等信息自动生成</p>\n"},"label":{"desc":"<p>设置节点的文本标签，如果不设置，则不返回标签相关的数据，支持三种格式：</p>\n<ul>\n<li><code class=\"codespan\">boolean</code> 布尔类型，设置是否显示文本标签，对所有的层级生效</li>\n<li><code class=\"codespan\">SunburstLabelOptions</code> 设置所有层级的文本标签配置，配置的具体含义见下文</li>\n<li><code class=\"codespan\">SunburstLabelOptions[]</code> <code class=\"codespan\">label[i]</code>表示第 i 层的文本标签配置</li>\n</ul>\n<p><code class=\"codespan\">SunburstLabelOptions</code>的具体类型定义为：</p>\n<pre><code class=\"lang-ts\">interface SunburstLabelConfig {\n  align?: &#39;start&#39; | &#39;end&#39; | &#39;center&#39;;\n  rotate?: &#39;tangential&#39; | &#39;radial&#39;;\n  offset?: number;\n}\n</code></pre>\n<p>其中，每个参数的含义如下：</p>\n<ul>\n<li><code class=\"codespan\">align</code> 表示标签的对齐方式，<code class=\"codespan\">start</code> 表示沿着内径的方向对齐，<code class=\"codespan\">end</code> 表示沿着外径的方向对齐，<code class=\"codespan\">center</code> 表示沿着中间的半径对齐</li>\n<li><code class=\"codespan\">rotate</code> 表示标签的旋转方式，<code class=\"codespan\">tangetial</code> 表示沿着切线方向旋转，<code class=\"codespan\">radial</code> 表示沿着半径方向旋转</li>\n<li><code class=\"codespan\">offset</code> 表示标签的偏移量，单位为<code class=\"codespan\">px</code></li>\n</ul>\n"}}